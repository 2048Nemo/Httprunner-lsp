{
	"name": "httprunner-lsp",
	"description": "一个支持 httprunner 框架的 lsp 服务扩展，支持变量以及debugtalk 函数和方法跳转，以及查看函数签名，支持文件引用跳转",
	"author": "2048Nemo",
	"license": "MIT",
	"version": "1.0.1",
	"repository": {
		"type": "git",
		"url": "https://github.com/Microsoft/vscode-extension-samples"
	},
	"publisher": "NemoLexist",
	"categories": [],
	"keywords": [
		"multi-root ready"
	],
	"engines": {
		"vscode": "^1.90.0"
	},
	"activationEvents": [
		"onLanguage:yaml",
		"onLanguage:python",
		"onLanguage:env"
	],
	"main": "./client/out/extension",
	"contributes": {
		"commands": [
			{
				"command": "my-hrun-extension.runHrunScript",
				"title": "使用 hrun 运行",
				"category": "Hrun"
			}
		],
		"menus": {
			"commandPalette": [
				{
					"command": "my-hrun-extension.runHrunScript",
					"when": "editorTextFocus"
				}
			],
			"editor/context": [
				{
					"when": "resourceLangId == yaml",
					"command": "my-hrun-extension.runHrunScript",
					"group": "navigation@1"
				}
			],
			"explorer/context": [
				{
					"when": "resourceExtname == .yml || resourceExtname == .yaml",
					"command": "my-hrun-extension.runHrunScript",
					"group": "navigation@1"
				}
			]
		},
		"configuration": {
			"type": "object",
			"title": "Example configuration",
			"properties": {
				"languageServerExample.maxNumberOfProblems": {
					"scope": "resource",
					"type": "number",
					"default": 100,
					"description": "Controls the maximum number of problems produced by the server."
				},
				"languageServerExample.trace.server": {
					"scope": "window",
					"type": "string",
					"enum": [
						"off",
						"messages",
						"verbose"
					],
					"default": "verbose",
					"description": "Traces the communication between VS Code and the language server."
				}
			},
			"languages": [
				{
					"id": "yaml",
					"extensions": [
						".yaml",
						".yml"
					],
					"aliases": [
						"YAML",
						"YML",
						"yaml",
						"yml"
					]
				},
				{
					"id": "python",
					"extensions": [
						".py"
					],
					"aliases": [
						"Python",
						"python"
					]
				},
				{
					"id": "env",
					"extensions": [
						".env"
					],
					"aliases": [
						"Env",
						"env"
					]
				}
			]
		}
	},
	"scripts": {
		"vscode:prepublish": "npm run compile",
		"compile": "tsc -b",
		"watch": "tsc -b -w",
		"lint": "eslint",
		"postinstall": "cd client && npm install && cd ../server && npm install && cd ..",
		"test": "sh ./scripts/e2e.sh"
	},
	"devDependencies": {
		"@eslint/js": "^9.13.0",
		"@stylistic/eslint-plugin": "^2.9.0",
		"@types/mocha": "^10.0.6",
		"@types/node": "^20",
		"eslint": "^9.33.0",
		"mocha": "^10.3.0",
		"typescript": "^5.8.2",
		"typescript-eslint": "^8.26.0"
	},
	"dependencies": {
		"vscode-uri": "^3.1.0",
		"yaml": "^2.8.1"
	}
}